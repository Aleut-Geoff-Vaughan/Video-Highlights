[pytest]
# Make the repo root importable (backend/, VideoHighlights.py) even when
# pytest is invoked as a bare binary (CI) rather than `python -m pytest`.
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
